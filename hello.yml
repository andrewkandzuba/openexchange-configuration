server:
  port: 8081
configuration:
  properties:
    language: "NL"
logging:
  level:
    org.springframework.web: "INFO"
spring:
  cloud:
    config:
      refresh:
        rate: 10
        timeUnit: SECONDS